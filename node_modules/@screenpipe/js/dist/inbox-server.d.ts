import express from 'express';

declare const actionCallbacks: Map<string, () => Promise<void>>;
declare function startInboxServer(port: number): Promise<express.Express>;

export { actionCallbacks, startInboxServer };
